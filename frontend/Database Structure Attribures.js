// player(id, player_name, email, password, type, profile, token);
// Player_info(id, profile, gold_tokens, diamond_tokens, bio);
// competition(id, game_id, name, icon, Timestamp, desc, status);
// player-competition(player_id, competition_id);
// ChargeToken(id, player_id, Offer_id, price_paied, tokens_get);
// Offer(id, name, desc, new price, old price, token_nbr);
// Game(id, name, icon, desc, status);